import Navbar from "../components/navbar";

function Resume({mobileNavMode, setMobileNavMode}) {
    return (
        <div>
            <Navbar></Navbar>
            <p>resume</p>
        </div>
    );
};

export default Resume;